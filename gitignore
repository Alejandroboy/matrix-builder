# Секреты. .env содержит ключи YooKassa, SMTP и DATABASE_URL — в git не попадает никогда.
.env
.env.*
!.env.example

# Зависимости и сборка
node_modules/
dist/
packages/web/.next/
packages/web/tsconfig.tsbuildinfo
.yarn/install-state.gz

# ОС и редакторы
.DS_Store
.idea/
*.log