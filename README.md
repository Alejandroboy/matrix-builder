# Матрица — монорепа

## Статус
- engine: ГОТОВ, 8/8 тестов. Схема расчёта подтверждена источником (см. ниже).
- docgen: схема контента, загрузчик (fail-fast), правила пар, компилятор
  отчёта совместимости + snapshot-тест; render-pdf ГОТОВ (pdfkit,
  кириллица через встраиваемый TTF) + дымовой тест с проверкой
  текстового слоя через pdftotext.
- content: арканы 3, 4, 5, 6, 11, 13 полностью (карточка + 5 блоков
  прозы) — 6 из 22. content/drafts/ — staging-зона карточек, ждущих
  прозы (загрузчик видит только content/arcana/); сейчас пуста.
  pair-rules.json: темпераменты всех 22 (DRAFT), 15 правил-каркасов [DRAFT].
- web: ИНТЕГРИРОВАН (адаптация твоего форка Неустойки). Платёжное ядро
  (yookassa.ts, полинг статуса, download-token, webhook, prisma.ts) —
  БЕЗ ИЗМЕНЕНИЙ. Адаптировано: schema.prisma (productType, input, Lead),
  order.ts (цены 290/390), validation.ts (через движок), orders route
  (+проверка покрытия контентом, 409 -> лид-форма), leads route (новый),
  download route (PDF вместо docx), Calculator.tsx (заменён; старый
  сохранён как Calculator.neustoyka.bak.txt), страницы /, /sovmestimost,
  /arkan/[n] (SSG из контентных JSON, индексируются только готовые арканы).
  Typecheck чистый. Шрифты: assets/fonts/PT Serif (OFL, из google/fonts).
  ПЕРЕД ЗАПУСКОМ: npx prisma migrate dev --name matrix_init (старая
  миграция Неустойки в prisma/migrations несовместима — удалить папку
  миграций и создать заново, база новая).

## Схема расчёта (зафиксирована)
Доминирующая школа. a=день, b=месяц, c=digitSum(год), d=a+b+c, e=центр=a+b+c+d;
линия: d1=d+e, c1=c+e («вход в денежный канал»), x=d1+c1,
x1=«под сердцем»=d1+x, x2=«под долларом»=x+c1.
Редукция: сумма цифр пока >22 (НЕ вычитание 22 — школы расходятся, выбор зафиксирован).
Источник вывода: tvoyamatritsa.ru, статья «Деньги в матрице судьбы»,
их пример 21.11.1986 включён в golden-тесты как референс.

## ⚠️ Открытые проверки перед продажей
1. Кросс-чек личной матрицы на 2-3 калькуляторах конкурентов по golden-датам
   (matrica-sudby.ru, human-design.space) — схема сверена с одним источником.
2. Joint-формулы: принцип подтверждён источником (сложение одинаковых
   позиций двух матриц); остался численный кросс-чек на 1-2 калькуляторах
   конкурентов руками + golden в compatibility.test.ts.
3. pair-rules: заменить [DRAFT]-тексты, ревизовать темпераменты.

## PDF
Шрифты передаются в renderCompatibilityPdf снаружи (RenderOptions.fonts) —
в тестах системный DejaVu, в проде положи брендовые TTF (PT Serif/PT Sans)
в ассеты web. Стандартные PDF-шрифты кириллицу НЕ содержат.

## Команды
yarn install
yarn workspace @matrix/engine build   # обязателен до тестов docgen
yarn test
