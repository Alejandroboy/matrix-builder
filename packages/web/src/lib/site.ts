/**
 * Канонический адрес сайта. Одно место на всё приложение: sitemap, robots,
 * канонические ссылки в метаданных.
 *
 * Без www — выбран как основной. Вариант с www отдаёт 301 на этот адрес,
 * иначе поисковик видит два сайта с одинаковым содержимым и делит между
 * ними вес.
 */
export const SITE_URL = process.env.APP_URL?.replace(/\/$/, '') ?? 'https://matricasudba.ru';
